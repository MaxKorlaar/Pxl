<!DOCTYPE html>
<html class="error-page">
<head>
    {% include 'partials/head.twig' %}
    <title>Error 500 – {{ config_get('app.name') }}</title>
</head>
<body class="error-page">
<div class="container">
    <div class="content">
        <img src="{{ asset('img/logo/Px-logo-cut.png') }}">
        <div class="title">{{ config_get('app.debug') ? exception.message : trans('error.something_went_wrong') }}</div>
    </div>
</div>
</body>
</html>