{% extends 'partials/page.twig' %}
{% block title %}{{ trans('admin.users.title') }}{% endblock %}
{% block content %}
    <div class="section">
        <div class="row">
            <div class="col s12">
                <h1>{{ trans('admin.users.title') }}</h1>
                {{ dump(users, t) }}
            </div>
        </div>
    </div>
{% endblock %}