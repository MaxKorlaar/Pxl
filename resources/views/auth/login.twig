{% extends 'empty_page.twig' %}
{% block title %}Log in{% endblock %}
{% block content %}
    <div class="row">
        <div class="col s12 m6 offset-m3">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">Log in &bull; {{ config_get('app.name') }}</span>

                    <p>Login form should be around here.</p>
                </div>
                <div class="card-action">
                    <a href="#">Log in</a>
                    <a href="#">Forgot password?</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}